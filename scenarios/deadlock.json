{
    "isolation": "REPEATABLE READ",
    "transactions": {
        "T1": [
            {"step": 1, "query": "SELECT balance FROM accounts WHERE account_id = 1 FOR UPDATE;"},
            {"step": 3, "query": "UPDATE accounts SET balance = balance - 50 WHERE account_id = 2;"},
            {"step": 5, "query": "COMMIT;"}
        ],
        "T2": [
            {"step": 2, "query": "SELECT balance FROM accounts WHERE account_id = 2 FOR UPDATE;"},
            {"step": 4, "query": "UPDATE accounts SET balance = balance - 50 WHERE account_id = 1;"},
            {"step": 6, "query": "COMMIT;"}
        ]
    }
}
